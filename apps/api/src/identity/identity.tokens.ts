/** Tokens de inyección de la capa de identidad. */
export const ENV = Symbol("Env");
/** `true` cuando las cookies llevan `Secure` y el prefijo `__Host-`. */
export const SECURE_COOKIES = Symbol("SecureCookies");
